class Rectangle {
    double length;
    double breadth;

    public Rectangle(double l, double b) {
        this.length = l;
        this.breadth = b;
    }

    void display() 
    {
        a = (length * breadth);
        System.out.println("Area: " + a);
    }

    public static void main(String args[]) 
    {
        Rect r = new Rect();
        r.display();
       
    }
}